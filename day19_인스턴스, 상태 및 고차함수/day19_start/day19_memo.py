# Day19: More Turtle Graphics, Event Listeners, State and Multiple Instances

# Higher Order Function(고차함수)
# 다른 함수와 함께 작동하는 함수
# def calculator(n1, n2, func):
#   return func(n1, n2)
# result = calculator(2, 3, add)

# Instance
# 하나의 Class를 통해 생성한 n개의 객체를 별개의 인스턴스라고 부름
# 각 개체가 어느 한 순간에 다른 속성을 지닐 수 있으며, 각기 다른 메소드를 수행할 수 있다는 사실을
# 객체의 상태(State) 라고 부름
# 예) turtle 클래스의 객체 timmy의 색상 속성 상태는 초록색이고, tommy의 색상 속성은 보라색
# 두 객체가 속성 즉 모양 면에서 다른 상태를 지니고 있음
